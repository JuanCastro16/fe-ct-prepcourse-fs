/*⚠️ NO MODIFIQUES EL NOMBRE DE LAS DECLARACIONES ⚠️*/
/*2️⃣ EJERCICIO 02 2️⃣*/

function devolverString(string) {
   // Debe retornar un string.
   // Tu código:
   return string
}

// ⛔️ "X" e "Y" son números.

function suma(x, y) {
   // Debe retornar una suma
   // Tu código:
   return x + y
}

function resta(x, y) {
   // Debe retornar una resta
   // Tu código:
   return x - y
}

function divide(x, y) {
   // Debe retornar una division
   // Tu código:
   return x / y
}

function multiplica(x, y) {
   // Debe retornar una multiplicacion
   // Tu código
   return x * y
}

function obtenerResto(x, y) {
   // Debe retornar el resto
   // Tu código:
   return x % y
}

/*⚠️ NO MODIFIQUES NADA DEBAJO DE ESTO ⚠️*/
module.exports = {
   devolverString,
   suma,
   resta,
   divide,
   multiplica,
   obtenerResto,
};
